# Backlog (не загружается агентом по умолчанию)

Перенесено из SKILL.md, чтобы не засорять рабочий поток навыка. Этот файл — parking lot для идей, которые ещё не превратились в инструкции.

## TODO

- [ ] Скрипт для автоматической распаковки epub и очистки HTML (сборка spine-порядка из `content.opf` в один файл — см. Фазу 0).
- [ ] Скрипт миграции старой структуры базы знаний → `author/title/` (вероятно уже выполнен — проверьте актуальность перед реализацией).
